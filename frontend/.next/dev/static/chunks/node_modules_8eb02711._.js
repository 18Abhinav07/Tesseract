(globalThis.TURBOPACK || (globalThis.TURBOPACK = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/@polkadot/rpc-provider/coder/error.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>RpcError
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/function.js [app-client] (ecmascript)");
;
const UNKNOWN = -99999;
function extend(that, name, value) {
    Object.defineProperty(that, name, {
        configurable: true,
        enumerable: false,
        value
    });
}
class RpcError extends Error {
    code;
    data;
    message;
    name;
    stack;
    constructor(message = '', code = UNKNOWN, data){
        super();
        extend(this, 'message', String(message));
        extend(this, 'name', this.constructor.name);
        extend(this, 'data', data);
        extend(this, 'code', code);
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isFunction"])(Error.captureStackTrace)) {
            Error.captureStackTrace(this, this.constructor);
        } else {
            const { stack } = new Error(message);
            stack && extend(this, 'stack', stack);
        }
    }
    static CODES = {
        ASSERT: -90009,
        INVALID_JSONRPC: -99998,
        METHOD_NOT_FOUND: -32601,
        UNKNOWN
    };
}
}),
"[project]/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RpcCoder",
    ()=>RpcCoder
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/number.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/string.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/undefined.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/coder/error.js [app-client] (ecmascript)");
;
;
function formatErrorData(data) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(data)) {
        return '';
    }
    const formatted = `: ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isString"])(data) ? data.replace(/Error\("/g, '').replace(/\("/g, '(').replace(/"\)/g, ')').replace(/\(/g, ', ').replace(/\)/g, '') : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(data)}`;
    // We need some sort of cut-off here since these can be very large and
    // very nested, pick a number and trim the result display to it
    return formatted.length <= 256 ? formatted : `${formatted.substring(0, 255)}…`;
}
function checkError(error) {
    if (error) {
        const { code, data, message } = error;
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"](`${code}: ${message}${formatErrorData(data)}`, code, data);
    }
}
class RpcCoder {
    #id = 0;
    decodeResponse(response) {
        if (!response || response.jsonrpc !== '2.0') {
            throw new Error('Invalid jsonrpc field in decoded object');
        }
        const isSubscription = !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.params) && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.method);
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNumber"])(response.id) && (!isSubscription || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNumber"])(response.params.subscription) && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isString"])(response.params.subscription))) {
            throw new Error('Invalid id field in decoded object');
        }
        checkError(response.error);
        if (response.result === undefined && !isSubscription) {
            throw new Error('No result found in jsonrpc response');
        }
        if (isSubscription) {
            checkError(response.params.error);
            return response.params.result;
        }
        return response.result;
    }
    encodeJson(method, params) {
        const [id, data] = this.encodeObject(method, params);
        return [
            id,
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(data)
        ];
    }
    encodeObject(method, params) {
        const id = ++this.#id;
        return [
            id,
            {
                id,
                jsonrpc: '2.0',
                method,
                params
            }
        ];
    }
}
}),
"[project]/node_modules/@polkadot/rpc-provider/defaults.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
const HTTP_URL = 'http://127.0.0.1:9933';
const WS_URL = 'ws://127.0.0.1:9944';
const __TURBOPACK__default__export__ = {
    HTTP_URL,
    WS_URL
};
}),
"[project]/node_modules/@polkadot/rpc-provider/lru.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_CAPACITY",
    ()=>DEFAULT_CAPACITY,
    "DEFAULT_TTL",
    ()=>DEFAULT_TTL,
    "LRUCache",
    ()=>LRUCache
]);
const DEFAULT_CAPACITY = 1024;
const DEFAULT_TTL = 30000; // 30 seconds
const MAX_TTL = 1800_000; // 30 minutes
const DISABLED_TTL = 31_536_000_000;
class LRUNode {
    key;
    #expires;
    #ttl;
    createdAt;
    next;
    prev;
    constructor(key, ttl){
        this.key = key;
        this.#ttl = ttl;
        this.#expires = Date.now() + ttl;
        this.createdAt = Date.now();
        this.next = this.prev = this;
    }
    refresh() {
        this.#expires = Date.now() + this.#ttl;
    }
    get expiry() {
        return this.#expires;
    }
}
class LRUCache {
    capacity;
    #data = new Map();
    #refs = new Map();
    #length = 0;
    #head;
    #tail;
    #ttl;
    constructor(capacity = DEFAULT_CAPACITY, ttl = DEFAULT_TTL){
        // Validate capacity
        if (!Number.isInteger(capacity) || capacity < 0) {
            throw new Error(`LRUCache initialization error: 'capacity' must be a non-negative integer. Received: ${capacity}`);
        }
        // Validate ttl
        if (ttl !== null && (!Number.isFinite(ttl) || ttl < 0 || ttl > MAX_TTL)) {
            throw new Error(`LRUCache initialization error: 'ttl' must be between 0 and ${MAX_TTL} ms or null to disable. Received: ${ttl}`);
        }
        this.capacity = capacity;
        ttl ? this.#ttl = ttl : this.#ttl = DISABLED_TTL;
        this.#head = this.#tail = new LRUNode('<empty>', this.#ttl);
    }
    get ttl() {
        return this.#ttl;
    }
    get length() {
        return this.#length;
    }
    get lengthData() {
        return this.#data.size;
    }
    get lengthRefs() {
        return this.#refs.size;
    }
    entries() {
        const keys = this.keys();
        const count = keys.length;
        const entries = new Array(count);
        for(let i = 0; i < count; i++){
            const key = keys[i];
            entries[i] = [
                key,
                this.#data.get(key)
            ];
        }
        return entries;
    }
    keys() {
        const keys = [];
        if (this.#length) {
            let curr = this.#head;
            while(curr !== this.#tail){
                keys.push(curr.key);
                curr = curr.next;
            }
            keys.push(curr.key);
        }
        return keys;
    }
    get(key) {
        const data = this.#data.get(key);
        if (data) {
            this.#toHead(key);
            // Evict TTL once data is refreshed
            this.#evictTTL();
            return data;
        }
        this.#evictTTL();
        return null;
    }
    set(key, value) {
        if (this.#data.has(key)) {
            this.#toHead(key);
        } else {
            const node = new LRUNode(key, this.#ttl);
            this.#refs.set(node.key, node);
            if (this.length === 0) {
                this.#head = this.#tail = node;
            } else {
                this.#head.prev = node;
                node.next = this.#head;
                this.#head = node;
            }
            if (this.#length === this.capacity) {
                this.#data.delete(this.#tail.key);
                this.#refs.delete(this.#tail.key);
                this.#tail = this.#tail.prev;
                this.#tail.next = this.#head;
            } else {
                this.#length += 1;
            }
        }
        // Evict TTL once data is refreshed or added
        this.#evictTTL();
        this.#data.set(key, value);
    }
    #evictTTL() {
        // Find last node to keep
        // traverse map to find the expired nodes
        while(this.#tail.expiry && this.#tail.expiry < Date.now() && this.#length > 0){
            this.#refs.delete(this.#tail.key);
            this.#data.delete(this.#tail.key);
            this.#length -= 1;
            this.#tail = this.#tail.prev;
            this.#tail.next = this.#head;
        }
        if (this.#length === 0) {
            this.#head = this.#tail = new LRUNode('<empty>', this.#ttl);
        }
    }
    #toHead(key) {
        const ref = this.#refs.get(key);
        if (ref && ref !== this.#head) {
            ref.refresh();
            ref.prev.next = ref.next;
            ref.next.prev = ref.prev;
            ref.next = this.#head;
            this.#head.prev = ref;
            this.#head = ref;
        }
    }
}
}),
"[project]/node_modules/@polkadot/rpc-provider/ws/errors.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getWSErrorString",
    ()=>getWSErrorString
]);
const known = {
    1000: 'Normal Closure',
    1001: 'Going Away',
    1002: 'Protocol Error',
    1003: 'Unsupported Data',
    1004: '(For future)',
    1005: 'No Status Received',
    1006: 'Abnormal Closure',
    1007: 'Invalid frame payload data',
    1008: 'Policy Violation',
    1009: 'Message too big',
    1010: 'Missing Extension',
    1011: 'Internal Error',
    1012: 'Service Restart',
    1013: 'Try Again Later',
    1014: 'Bad Gateway',
    1015: 'TLS Handshake'
};
function getWSErrorString(code) {
    if (code >= 0 && code <= 999) {
        return '(Unused)';
    } else if (code >= 1016) {
        if (code <= 1999) {
            return '(For WebSocket standard)';
        } else if (code <= 2999) {
            return '(For WebSocket extensions)';
        } else if (code <= 3999) {
            return '(For libraries and frameworks)';
        } else if (code <= 4999) {
            return '(For applications)';
        }
    }
    return known[code] || '(Unknown)';
}
}),
"[project]/node_modules/@polkadot/rpc-provider/ws/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "WsProvider",
    ()=>WsProvider
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.mjs [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.js [app-client] (ecmascript) <export default as EventEmitter>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$childClass$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/childClass.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$null$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/null.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/is/undefined.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/logger.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/noop.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/object/spread.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/node_modules/@polkadot/x-global/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/x-ws/browser.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/defaults.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/lru.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$ws$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/rpc-provider/ws/errors.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
const ALIASES = {
    chain_finalisedHead: 'chain_finalizedHead',
    chain_subscribeFinalisedHeads: 'chain_subscribeFinalizedHeads',
    chain_unsubscribeFinalisedHeads: 'chain_unsubscribeFinalizedHeads'
};
const RETRY_DELAY = 2_500;
const DEFAULT_TIMEOUT_MS = 60 * 1000;
const TIMEOUT_INTERVAL = 5_000;
const l = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["logger"])('api-ws');
/** @internal Clears a Record<*> of all keys, optionally with all callback on clear */ function eraseRecord(record, cb) {
    Object.keys(record).forEach((key)=>{
        if (cb) {
            cb(record[key]);
        }
        delete record[key];
    });
}
/** @internal Creates a default/empty stats object */ function defaultEndpointStats() {
    return {
        bytesRecv: 0,
        bytesSent: 0,
        cached: 0,
        errors: 0,
        requests: 0,
        subscriptions: 0,
        timeout: 0
    };
}
class WsProvider {
    #callCache;
    #coder;
    #endpoints;
    #headers;
    #eventemitter;
    #handlers = {};
    #isReadyPromise;
    #stats;
    #waitingForId = {};
    #cacheCapacity;
    #ttl;
    #autoConnectMs;
    #endpointIndex;
    #endpointStats;
    #isConnected = false;
    #subscriptions = {};
    #timeoutId = null;
    #websocket;
    #timeout;
    /**
     * @param {string | string[]} endpoint The endpoint url. Usually `ws://ip:9944` or `wss://ip:9944`, may provide an array of endpoint strings.
     * @param {number | false} autoConnectMs Whether to connect automatically or not (default). Provided value is used as a delay between retries.
     * @param {Record<string, string>} headers The headers provided to the underlying WebSocket
     * @param {number} [timeout] Custom timeout value used per request . Defaults to `DEFAULT_TIMEOUT_MS`
     * @param {number} [cacheCapacity] Custom size of the WsProvider LRUCache. Defaults to `DEFAULT_CAPACITY` (1024)
     * @param {number} [cacheTtl] Custom TTL of the WsProvider LRUCache. Determines how long an object can live in the cache. Defaults to DEFAULT_TTL` (30000)
     */ constructor(endpoint = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].WS_URL, autoConnectMs = RETRY_DELAY, headers = {}, timeout, cacheCapacity, cacheTtl){
        const endpoints = Array.isArray(endpoint) ? endpoint : [
            endpoint
        ];
        if (endpoints.length === 0) {
            throw new Error('WsProvider requires at least one Endpoint');
        }
        endpoints.forEach((endpoint)=>{
            if (!/^(wss|ws):\/\//.test(endpoint)) {
                throw new Error(`Endpoint should start with 'ws://', received '${endpoint}'`);
            }
        });
        const ttl = cacheTtl === undefined ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_TTL"] : cacheTtl;
        this.#callCache = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LRUCache"](cacheCapacity === 0 ? 0 : cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"], ttl);
        this.#ttl = cacheTtl;
        this.#cacheCapacity = cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"];
        this.#eventemitter = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__["EventEmitter"]();
        this.#autoConnectMs = autoConnectMs || 0;
        this.#coder = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RpcCoder"]();
        this.#endpointIndex = -1;
        this.#endpoints = endpoints;
        this.#headers = headers;
        this.#websocket = null;
        this.#stats = {
            active: {
                requests: 0,
                subscriptions: 0
            },
            total: defaultEndpointStats()
        };
        this.#endpointStats = defaultEndpointStats();
        this.#timeout = timeout || DEFAULT_TIMEOUT_MS;
        if (autoConnectMs && autoConnectMs > 0) {
            this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
        }
        this.#isReadyPromise = new Promise((resolve)=>{
            this.#eventemitter.once('connected', ()=>{
                resolve(this);
            });
        });
    }
    /**
     * @summary `true` when this provider supports subscriptions
     */ get hasSubscriptions() {
        return !!true;
    }
    /**
     * @summary `true` when this provider supports clone()
     */ get isClonable() {
        return !!true;
    }
    /**
     * @summary Whether the node is connected or not.
     * @return {boolean} true if connected
     */ get isConnected() {
        return this.#isConnected;
    }
    /**
     * @description Promise that resolves the first time we are connected and loaded
     */ get isReady() {
        return this.#isReadyPromise;
    }
    get endpoint() {
        return this.#endpoints[this.#endpointIndex];
    }
    /**
     * @description Returns a clone of the object
     */ clone() {
        return new WsProvider(this.#endpoints);
    }
    selectEndpointIndex(endpoints) {
        return (this.#endpointIndex + 1) % endpoints.length;
    }
    /**
     * @summary Manually connect
     * @description The [[WsProvider]] connects automatically by default, however if you decided otherwise, you may
     * connect manually using this method.
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async connect() {
        if (this.#websocket) {
            throw new Error('WebSocket is already connected');
        }
        try {
            this.#endpointIndex = this.selectEndpointIndex(this.#endpoints);
            // the as here is Deno-specific - not available on the globalThis
            this.#websocket = typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["xglobal"].WebSocket !== 'undefined' && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$childClass$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isChildClass"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["xglobal"].WebSocket, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"]) ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"](this.endpoint) : new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"](this.endpoint, undefined, {
                headers: this.#headers
            });
            if (this.#websocket) {
                this.#websocket.onclose = this.#onSocketClose;
                this.#websocket.onerror = this.#onSocketError;
                this.#websocket.onmessage = this.#onSocketMessage;
                this.#websocket.onopen = this.#onSocketOpen;
            }
            // timeout any handlers that have not had a response
            this.#timeoutId = setInterval(()=>this.#timeoutHandlers(), TIMEOUT_INTERVAL);
        } catch (error) {
            l.error(error);
            this.#emit('error', error);
            throw error;
        }
    }
    /**
     * @description Connect, never throwing an error, but rather forcing a retry
     */ async connectWithRetry() {
        if (this.#autoConnectMs > 0) {
            try {
                await this.connect();
            } catch  {
                setTimeout(()=>{
                    this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
                }, this.#autoConnectMs);
            }
        }
    }
    /**
     * @description Manually disconnect from the connection, clearing auto-connect logic
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async disconnect() {
        // switch off autoConnect, we are in manual mode now
        this.#autoConnectMs = 0;
        try {
            if (this.#websocket) {
                // 1000 - Normal closure; the connection successfully completed
                this.#websocket.close(1000);
            }
        } catch (error) {
            l.error(error);
            this.#emit('error', error);
            throw error;
        }
    }
    /**
     * @description Returns the connection stats
     */ get stats() {
        return {
            active: {
                requests: Object.keys(this.#handlers).length,
                subscriptions: Object.keys(this.#subscriptions).length
            },
            total: this.#stats.total
        };
    }
    /**
    * @description Returns the connection stats
    */ get ttl() {
        return this.#ttl;
    }
    get endpointStats() {
        return this.#endpointStats;
    }
    /**
     * @summary Listens on events after having subscribed using the [[subscribe]] function.
     * @param  {ProviderInterfaceEmitted} type Event
     * @param  {ProviderInterfaceEmitCb}  sub  Callback
     * @return unsubscribe function
     */ on(type, sub) {
        this.#eventemitter.on(type, sub);
        return ()=>{
            this.#eventemitter.removeListener(type, sub);
        };
    }
    /**
     * @summary Send JSON data using WebSockets to configured HTTP Endpoint or queue.
     * @param method The RPC methods to execute
     * @param params Encoded parameters as applicable for the method
     * @param subscription Subscription details (internally used)
     */ send(method, params, isCacheable, subscription) {
        this.#endpointStats.requests++;
        this.#stats.total.requests++;
        const [id, body] = this.#coder.encodeJson(method, params);
        if (this.#cacheCapacity === 0) {
            return this.#send(id, body, method, params, subscription);
        }
        const cacheKey = isCacheable ? `${method}::${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(params)}` : '';
        let resultPromise = isCacheable ? this.#callCache.get(cacheKey) : null;
        if (!resultPromise) {
            resultPromise = this.#send(id, body, method, params, subscription);
            if (isCacheable) {
                this.#callCache.set(cacheKey, resultPromise);
            }
        } else {
            this.#endpointStats.cached++;
            this.#stats.total.cached++;
        }
        return resultPromise;
    }
    async #send(id, body, method, params, subscription) {
        return new Promise((resolve, reject)=>{
            try {
                if (!this.isConnected || this.#websocket === null) {
                    throw new Error('WebSocket is not connected');
                }
                const callback = (error, result)=>{
                    error ? reject(error) : resolve(result);
                };
                l.debug(()=>[
                        'calling',
                        method,
                        body
                    ]);
                this.#handlers[id] = {
                    callback,
                    method,
                    params,
                    start: Date.now(),
                    subscription
                };
                const bytesSent = body.length;
                this.#endpointStats.bytesSent += bytesSent;
                this.#stats.total.bytesSent += bytesSent;
                this.#websocket.send(body);
            } catch (error) {
                this.#endpointStats.errors++;
                this.#stats.total.errors++;
                const rpcError = error;
                const failedRequest = `\nFailed WS Request: ${JSON.stringify({
                    method,
                    params
                })}`;
                // Provide WS Request alongside the error
                rpcError.message = `${rpcError.message}${failedRequest}`;
                reject(rpcError);
            }
        });
    }
    /**
     * @name subscribe
     * @summary Allows subscribing to a specific event.
     *
     * @example
     * <BR>
     *
     * ```javascript
     * const provider = new WsProvider('ws://127.0.0.1:9944');
     * const rpc = new Rpc(provider);
     *
     * rpc.state.subscribeStorage([[storage.system.account, <Address>]], (_, values) => {
     *   console.log(values)
     * }).then((subscriptionId) => {
     *   console.log('balance changes subscription id: ', subscriptionId)
     * })
     * ```
     */ subscribe(type, method, params, callback) {
        this.#endpointStats.subscriptions++;
        this.#stats.total.subscriptions++;
        // subscriptions are not cached, LRU applies to .at(<blockHash>) only
        return this.send(method, params, false, {
            callback,
            type
        });
    }
    /**
     * @summary Allows unsubscribing to subscriptions made with [[subscribe]].
     */ async unsubscribe(type, method, id) {
        const subscription = `${type}::${id}`;
        // FIXME This now could happen with re-subscriptions. The issue is that with a re-sub
        // the assigned id now does not match what the API user originally received. It has
        // a slight complication in solving - since we cannot rely on the send id, but rather
        // need to find the actual subscription id to map it
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(this.#subscriptions[subscription])) {
            l.debug(()=>`Unable to find active subscription=${subscription}`);
            return false;
        }
        delete this.#subscriptions[subscription];
        try {
            return this.isConnected && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$null$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNull"])(this.#websocket) ? this.send(method, [
                id
            ]) : true;
        } catch  {
            return false;
        }
    }
    #emit = (type, ...args)=>{
        this.#eventemitter.emit(type, ...args);
    };
    #onSocketClose = (event)=>{
        const error = new Error(`disconnected from ${this.endpoint}: ${event.code}:: ${event.reason || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$ws$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getWSErrorString"])(event.code)}`);
        if (this.#autoConnectMs > 0) {
            l.error(error.message);
        }
        this.#isConnected = false;
        if (this.#websocket) {
            this.#websocket.onclose = null;
            this.#websocket.onerror = null;
            this.#websocket.onmessage = null;
            this.#websocket.onopen = null;
            this.#websocket = null;
        }
        if (this.#timeoutId) {
            clearInterval(this.#timeoutId);
            this.#timeoutId = null;
        }
        // reject all hanging requests
        eraseRecord(this.#handlers, (h)=>{
            try {
                h.callback(error, undefined);
            } catch (err) {
                // does not throw
                l.error(err);
            }
        });
        eraseRecord(this.#waitingForId);
        // Reset stats for active endpoint
        this.#endpointStats = defaultEndpointStats();
        this.#emit('disconnected');
        if (this.#autoConnectMs > 0) {
            setTimeout(()=>{
                this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
            }, this.#autoConnectMs);
        }
    };
    #onSocketError = (error)=>{
        l.debug(()=>[
                'socket error',
                error
            ]);
        this.#emit('error', error);
    };
    #onSocketMessage = (message)=>{
        l.debug(()=>[
                'received',
                message.data
            ]);
        const bytesRecv = message.data.length;
        this.#endpointStats.bytesRecv += bytesRecv;
        this.#stats.total.bytesRecv += bytesRecv;
        const response = JSON.parse(message.data);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.method) ? this.#onSocketMessageResult(response) : this.#onSocketMessageSubscribe(response);
    };
    #onSocketMessageResult = (response)=>{
        const handler = this.#handlers[response.id];
        if (!handler) {
            l.debug(()=>`Unable to find handler for id=${response.id}`);
            return;
        }
        try {
            const { method, params, subscription } = handler;
            const result = this.#coder.decodeResponse(response);
            // first send the result - in case of subs, we may have an update
            // immediately if we have some queued results already
            handler.callback(null, result);
            if (subscription) {
                const subId = `${subscription.type}::${result}`;
                this.#subscriptions[subId] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["objectSpread"])({}, subscription, {
                    method,
                    params
                });
                // if we have a result waiting for this subscription already
                if (this.#waitingForId[subId]) {
                    this.#onSocketMessageSubscribe(this.#waitingForId[subId]);
                }
            }
        } catch (error) {
            this.#endpointStats.errors++;
            this.#stats.total.errors++;
            handler.callback(error, undefined);
        }
        delete this.#handlers[response.id];
    };
    #onSocketMessageSubscribe = (response)=>{
        if (!response.method) {
            throw new Error('No method found in JSONRPC response');
        }
        const method = ALIASES[response.method] || response.method;
        const subId = `${method}::${response.params.subscription}`;
        const handler = this.#subscriptions[subId];
        if (!handler) {
            // store the JSON, we could have out-of-order subid coming in
            this.#waitingForId[subId] = response;
            l.debug(()=>`Unable to find handler for subscription=${subId}`);
            return;
        }
        // housekeeping
        delete this.#waitingForId[subId];
        try {
            const result = this.#coder.decodeResponse(response);
            handler.callback(null, result);
        } catch (error) {
            this.#endpointStats.errors++;
            this.#stats.total.errors++;
            handler.callback(error, undefined);
        }
    };
    #onSocketOpen = ()=>{
        if (this.#websocket === null) {
            throw new Error('WebSocket cannot be null in onOpen');
        }
        l.debug(()=>[
                'connected to',
                this.endpoint
            ]);
        this.#isConnected = true;
        this.#resubscribe();
        this.#emit('connected');
        return true;
    };
    #resubscribe = ()=>{
        const subscriptions = this.#subscriptions;
        this.#subscriptions = {};
        Promise.all(Object.keys(subscriptions).map(async (id)=>{
            const { callback, method, params, type } = subscriptions[id];
            // only re-create subscriptions which are not in author (only area where
            // transactions are created, i.e. submissions such as 'author_submitAndWatchExtrinsic'
            // are not included (and will not be re-broadcast)
            if (type.startsWith('author_')) {
                return;
            }
            try {
                await this.subscribe(type, method, params, callback);
            } catch (error) {
                l.error(error);
            }
        })).catch(l.error);
    };
    #timeoutHandlers = ()=>{
        const now = Date.now();
        const ids = Object.keys(this.#handlers);
        for(let i = 0, count = ids.length; i < count; i++){
            const handler = this.#handlers[ids[i]];
            if (now - handler.start > this.#timeout) {
                try {
                    handler.callback(new Error(`No response received from RPC endpoint in ${this.#timeout / 1000}s`), undefined);
                } catch  {
                // ignore
                }
                this.#endpointStats.timeout++;
                this.#stats.total.timeout++;
                delete this.#handlers[ids[i]];
            }
        }
    };
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/packageInfo.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "packageInfo",
    ()=>packageInfo
]);
const __TURBOPACK__import$2e$meta__ = {
    get url () {
        return `file://${__turbopack_context__.P("node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/packageInfo.js")}`;
    }
};
const packageInfo = {
    name: '@polkadot/rpc-provider',
    path: __TURBOPACK__import$2e$meta__ && __TURBOPACK__import$2e$meta__.url ? new URL(__TURBOPACK__import$2e$meta__.url).pathname.substring(0, new URL(__TURBOPACK__import$2e$meta__.url).pathname.lastIndexOf('/') + 1) : 'auto',
    type: 'esm',
    version: '16.4.8'
};
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/error.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>RpcError
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/function.js [app-client] (ecmascript)");
;
const UNKNOWN = -99999;
function extend(that, name, value) {
    Object.defineProperty(that, name, {
        configurable: true,
        enumerable: false,
        value
    });
}
class RpcError extends Error {
    code;
    data;
    message;
    name;
    stack;
    constructor(message = '', code = UNKNOWN, data){
        super();
        extend(this, 'message', String(message));
        extend(this, 'name', this.constructor.name);
        extend(this, 'data', data);
        extend(this, 'code', code);
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isFunction"])(Error.captureStackTrace)) {
            Error.captureStackTrace(this, this.constructor);
        } else {
            const { stack } = new Error(message);
            stack && extend(this, 'stack', stack);
        }
    }
    static CODES = {
        ASSERT: -90009,
        INVALID_JSONRPC: -99998,
        METHOD_NOT_FOUND: -32601,
        UNKNOWN
    };
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "RpcCoder",
    ()=>RpcCoder
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/number.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/string.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/undefined.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/error.js [app-client] (ecmascript)");
;
;
function formatErrorData(data) {
    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(data)) {
        return '';
    }
    const formatted = `: ${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isString"])(data) ? data.replace(/Error\("/g, '').replace(/\("/g, '(').replace(/"\)/g, ')').replace(/\(/g, ', ').replace(/\)/g, '') : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(data)}`;
    // We need some sort of cut-off here since these can be very large and
    // very nested, pick a number and trim the result display to it
    return formatted.length <= 256 ? formatted : `${formatted.substring(0, 255)}…`;
}
function checkError(error) {
    if (error) {
        const { code, data, message } = error;
        throw new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"](`${code}: ${message}${formatErrorData(data)}`, code, data);
    }
}
class RpcCoder {
    #id = 0;
    decodeResponse(response) {
        if (!response || response.jsonrpc !== '2.0') {
            throw new Error('Invalid jsonrpc field in decoded object');
        }
        const isSubscription = !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.params) && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.method);
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNumber"])(response.id) && (!isSubscription || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$number$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNumber"])(response.params.subscription) && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$string$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isString"])(response.params.subscription))) {
            throw new Error('Invalid id field in decoded object');
        }
        checkError(response.error);
        if (response.result === undefined && !isSubscription) {
            throw new Error('No result found in jsonrpc response');
        }
        if (isSubscription) {
            checkError(response.params.error);
            return response.params.result;
        }
        return response.result;
    }
    encodeJson(method, params) {
        const [id, data] = this.encodeObject(method, params);
        return [
            id,
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(data)
        ];
    }
    encodeObject(method, params) {
        const id = ++this.#id;
        return [
            id,
            {
                id,
                jsonrpc: '2.0',
                method,
                params
            }
        ];
    }
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/defaults.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>__TURBOPACK__default__export__
]);
const HTTP_URL = 'http://127.0.0.1:9933';
const WS_URL = 'ws://127.0.0.1:9944';
const __TURBOPACK__default__export__ = {
    HTTP_URL,
    WS_URL
};
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/lru.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "DEFAULT_CAPACITY",
    ()=>DEFAULT_CAPACITY,
    "DEFAULT_TTL",
    ()=>DEFAULT_TTL,
    "LRUCache",
    ()=>LRUCache
]);
const DEFAULT_CAPACITY = 1024;
const DEFAULT_TTL = 30000; // 30 seconds
const MAX_TTL = 1800_000; // 30 minutes
const DISABLED_TTL = 31_536_000_000;
class LRUNode {
    key;
    #expires;
    #ttl;
    createdAt;
    next;
    prev;
    constructor(key, ttl){
        this.key = key;
        this.#ttl = ttl;
        this.#expires = Date.now() + ttl;
        this.createdAt = Date.now();
        this.next = this.prev = this;
    }
    refresh() {
        this.#expires = Date.now() + this.#ttl;
    }
    get expiry() {
        return this.#expires;
    }
}
class LRUCache {
    capacity;
    #data = new Map();
    #refs = new Map();
    #length = 0;
    #head;
    #tail;
    #ttl;
    constructor(capacity = DEFAULT_CAPACITY, ttl = DEFAULT_TTL){
        // Validate capacity
        if (!Number.isInteger(capacity) || capacity < 0) {
            throw new Error(`LRUCache initialization error: 'capacity' must be a non-negative integer. Received: ${capacity}`);
        }
        // Validate ttl
        if (ttl !== null && (!Number.isFinite(ttl) || ttl < 0 || ttl > MAX_TTL)) {
            throw new Error(`LRUCache initialization error: 'ttl' must be between 0 and ${MAX_TTL} ms or null to disable. Received: ${ttl}`);
        }
        this.capacity = capacity;
        ttl ? this.#ttl = ttl : this.#ttl = DISABLED_TTL;
        this.#head = this.#tail = new LRUNode('<empty>', this.#ttl);
    }
    get ttl() {
        return this.#ttl;
    }
    get length() {
        return this.#length;
    }
    get lengthData() {
        return this.#data.size;
    }
    get lengthRefs() {
        return this.#refs.size;
    }
    entries() {
        const keys = this.keys();
        const count = keys.length;
        const entries = new Array(count);
        for(let i = 0; i < count; i++){
            const key = keys[i];
            entries[i] = [
                key,
                this.#data.get(key)
            ];
        }
        return entries;
    }
    keys() {
        const keys = [];
        if (this.#length) {
            let curr = this.#head;
            while(curr !== this.#tail){
                keys.push(curr.key);
                curr = curr.next;
            }
            keys.push(curr.key);
        }
        return keys;
    }
    get(key) {
        const data = this.#data.get(key);
        if (data) {
            this.#toHead(key);
            // Evict TTL once data is refreshed
            this.#evictTTL();
            return data;
        }
        this.#evictTTL();
        return null;
    }
    set(key, value) {
        if (this.#data.has(key)) {
            this.#toHead(key);
        } else {
            const node = new LRUNode(key, this.#ttl);
            this.#refs.set(node.key, node);
            if (this.length === 0) {
                this.#head = this.#tail = node;
            } else {
                this.#head.prev = node;
                node.next = this.#head;
                this.#head = node;
            }
            if (this.#length === this.capacity) {
                this.#data.delete(this.#tail.key);
                this.#refs.delete(this.#tail.key);
                this.#tail = this.#tail.prev;
                this.#tail.next = this.#head;
            } else {
                this.#length += 1;
            }
        }
        // Evict TTL once data is refreshed or added
        this.#evictTTL();
        this.#data.set(key, value);
    }
    #evictTTL() {
        // Find last node to keep
        // traverse map to find the expired nodes
        while(this.#tail.expiry && this.#tail.expiry < Date.now() && this.#length > 0){
            this.#refs.delete(this.#tail.key);
            this.#data.delete(this.#tail.key);
            this.#length -= 1;
            this.#tail = this.#tail.prev;
            this.#tail.next = this.#head;
        }
        if (this.#length === 0) {
            this.#head = this.#tail = new LRUNode('<empty>', this.#ttl);
        }
    }
    #toHead(key) {
        const ref = this.#refs.get(key);
        if (ref && ref !== this.#head) {
            ref.refresh();
            ref.prev.next = ref.next;
            ref.next.prev = ref.prev;
            ref.next = this.#head;
            this.#head.prev = ref;
            this.#head = ref;
        }
    }
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/http/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HttpProvider",
    ()=>HttpProvider
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/logger.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/noop.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$fetch$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/x-fetch/browser.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/defaults.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/lru.js [app-client] (ecmascript)");
;
;
;
;
;
const ERROR_SUBSCRIBE = 'HTTP Provider does not have subscriptions, use WebSockets instead';
const l = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["logger"])('api-http');
class HttpProvider {
    #callCache;
    #cacheCapacity;
    #coder;
    #endpoint;
    #headers;
    #stats;
    #ttl;
    /**
     * @param {string} endpoint The endpoint url starting with http://
     * @param {Record<string, string>} headers The headers provided to the underlying Http Endpoint
     * @param {number} [cacheCapacity] Custom size of the HttpProvider LRUCache. Defaults to `DEFAULT_CAPACITY` (1024)
     * @param {number} [cacheTtl] Custom TTL of the HttpProvider LRUCache. Determines how long an object can live in the cache. Defaults to `DEFAULT_TTL` (30000)
     */ constructor(endpoint = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].HTTP_URL, headers = {}, cacheCapacity, cacheTtl){
        if (!/^(https|http):\/\//.test(endpoint)) {
            throw new Error(`Endpoint should start with 'http://' or 'https://', received '${endpoint}'`);
        }
        this.#coder = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RpcCoder"]();
        this.#endpoint = endpoint;
        this.#headers = headers;
        this.#cacheCapacity = cacheCapacity === 0 ? 0 : cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"];
        const ttl = cacheTtl === undefined ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_TTL"] : cacheTtl;
        this.#callCache = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LRUCache"](cacheCapacity === 0 ? 0 : cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"], ttl);
        this.#ttl = cacheTtl;
        this.#stats = {
            active: {
                requests: 0,
                subscriptions: 0
            },
            total: {
                bytesRecv: 0,
                bytesSent: 0,
                cached: 0,
                errors: 0,
                requests: 0,
                subscriptions: 0,
                timeout: 0
            }
        };
    }
    /**
     * @summary `true` when this provider supports subscriptions
     */ get hasSubscriptions() {
        return !!false;
    }
    /**
     * @description Returns a clone of the object
     */ clone() {
        return new HttpProvider(this.#endpoint, this.#headers);
    }
    /**
     * @description Manually connect from the connection
     */ async connect() {
    // noop
    }
    /**
     * @description Manually disconnect from the connection
     */ async disconnect() {
    // noop
    }
    /**
     * @description Returns the connection stats
     */ get stats() {
        return this.#stats;
    }
    /**
    * @description Returns the connection stats
    */ get ttl() {
        return this.#ttl;
    }
    /**
     * @summary `true` when this provider supports clone()
     */ get isClonable() {
        return !!true;
    }
    /**
     * @summary Whether the node is connected or not.
     * @return {boolean} true if connected
     */ get isConnected() {
        return !!true;
    }
    /**
     * @summary Events are not supported with the HttpProvider, see [[WsProvider]].
     * @description HTTP Provider does not have 'on' emitters. WebSockets should be used instead.
     */ on(_type, _sub) {
        l.error('HTTP Provider does not have \'on\' emitters, use WebSockets instead');
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"];
    }
    /**
     * @summary Send HTTP POST Request with Body to configured HTTP Endpoint.
     */ async send(method, params, isCacheable) {
        this.#stats.total.requests++;
        const [, body] = this.#coder.encodeJson(method, params);
        if (this.#cacheCapacity === 0) {
            return this.#send(body);
        }
        const cacheKey = isCacheable ? `${method}::${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(params)}` : '';
        let resultPromise = isCacheable ? this.#callCache.get(cacheKey) : null;
        if (!resultPromise) {
            resultPromise = this.#send(body);
            if (isCacheable) {
                this.#callCache.set(cacheKey, resultPromise);
            }
        } else {
            this.#stats.total.cached++;
        }
        return resultPromise;
    }
    async #send(body) {
        this.#stats.active.requests++;
        this.#stats.total.bytesSent += body.length;
        try {
            const response = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$fetch$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["fetch"])(this.#endpoint, {
                body,
                headers: {
                    Accept: 'application/json',
                    'Content-Length': `${body.length}`,
                    'Content-Type': 'application/json',
                    ...this.#headers
                },
                method: 'POST'
            });
            if (!response.ok) {
                throw new Error(`[${response.status}]: ${response.statusText}`);
            }
            const result = await response.text();
            this.#stats.total.bytesRecv += result.length;
            const decoded = this.#coder.decodeResponse(JSON.parse(result));
            this.#stats.active.requests--;
            return decoded;
        } catch (e) {
            this.#stats.active.requests--;
            this.#stats.total.errors++;
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            const { method, params } = JSON.parse(body);
            const rpcError = e;
            // eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
            const failedRequest = `\nFailed HTTP Request: ${JSON.stringify({
                method,
                params
            })}`;
            // Provide HTTP Request alongside the error
            rpcError.message = `${rpcError.message}${failedRequest}`;
            throw rpcError;
        }
    }
    /**
     * @summary Subscriptions are not supported with the HttpProvider, see [[WsProvider]].
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async subscribe(_types, _method, _params, _cb) {
        l.error(ERROR_SUBSCRIBE);
        throw new Error(ERROR_SUBSCRIBE);
    }
    /**
     * @summary Subscriptions are not supported with the HttpProvider, see [[WsProvider]].
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async unsubscribe(_type, _method, _id) {
        l.error(ERROR_SUBSCRIBE);
        throw new Error(ERROR_SUBSCRIBE);
    }
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/substrate-connect/Health.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "HealthCheckError",
    ()=>HealthCheckError,
    "healthChecker",
    ()=>healthChecker
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
;
function healthChecker() {
    // `null` if health checker is not started.
    let checker = null;
    let sendJsonRpc = null;
    return {
        responsePassThrough: (jsonRpcResponse)=>{
            if (checker === null) {
                return jsonRpcResponse;
            }
            return checker.responsePassThrough(jsonRpcResponse);
        },
        sendJsonRpc: (request)=>{
            if (!sendJsonRpc) {
                throw new Error('setSendJsonRpc must be called before sending requests');
            }
            if (checker === null) {
                sendJsonRpc(request);
            } else {
                checker.sendJsonRpc(request);
            }
        },
        setSendJsonRpc: (cb)=>{
            sendJsonRpc = cb;
        },
        start: (healthCallback)=>{
            if (checker !== null) {
                throw new Error("Can't start the health checker multiple times in parallel");
            } else if (!sendJsonRpc) {
                throw new Error('setSendJsonRpc must be called before starting the health checks');
            }
            checker = new InnerChecker(healthCallback, sendJsonRpc);
            checker.update(true);
        },
        stop: ()=>{
            if (checker === null) {
                return;
            } // Already stopped.
            checker.destroy();
            checker = null;
        }
    };
}
class InnerChecker {
    #healthCallback;
    #currentHealthCheckId = null;
    #currentHealthTimeout = null;
    #currentSubunsubRequestId = null;
    #currentSubscriptionId = null;
    #requestToSmoldot;
    #isSyncing = false;
    #nextRequestId = 0;
    constructor(healthCallback, requestToSmoldot){
        this.#healthCallback = healthCallback;
        this.#requestToSmoldot = (request)=>requestToSmoldot((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(request));
    }
    sendJsonRpc = (request)=>{
        // Replace the `id` in the request to prefix the request ID with `extern:`.
        let parsedRequest;
        try {
            parsedRequest = JSON.parse(request);
        } catch  {
            return;
        }
        if (parsedRequest.id) {
            const newId = 'extern:' + (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(parsedRequest.id);
            parsedRequest.id = newId;
        }
        this.#requestToSmoldot(parsedRequest);
    };
    responsePassThrough = (jsonRpcResponse)=>{
        let parsedResponse;
        try {
            parsedResponse = JSON.parse(jsonRpcResponse);
        } catch  {
            return jsonRpcResponse;
        }
        // Check whether response is a response to `system_health`.
        if (parsedResponse.id && this.#currentHealthCheckId === parsedResponse.id) {
            this.#currentHealthCheckId = null;
            // Check whether query was successful. It is possible for queries to fail for
            // various reasons, such as the client being overloaded.
            if (!parsedResponse.result) {
                this.update(false);
                return null;
            }
            this.#healthCallback(parsedResponse.result);
            this.#isSyncing = parsedResponse.result.isSyncing;
            this.update(false);
            return null;
        }
        // Check whether response is a response to the subscription or unsubscription.
        if (parsedResponse.id && this.#currentSubunsubRequestId === parsedResponse.id) {
            this.#currentSubunsubRequestId = null;
            // Check whether query was successful. It is possible for queries to fail for
            // various reasons, such as the client being overloaded.
            if (!parsedResponse.result) {
                this.update(false);
                return null;
            }
            if (this.#currentSubscriptionId) {
                this.#currentSubscriptionId = null;
            } else {
                this.#currentSubscriptionId = parsedResponse.result;
            }
            this.update(false);
            return null;
        }
        // Check whether response is a notification to a subscription.
        if (parsedResponse.params && this.#currentSubscriptionId && parsedResponse.params.subscription === this.#currentSubscriptionId) {
            // Note that after a successful subscription, a notification containing
            // the current best block is always returned. Considering that a
            // subscription is performed in response to a health check, calling
            // `startHealthCheck()` here will lead to a second health check.
            // It might seem redundant to perform two health checks in a quick
            // succession, but doing so doesn't lead to any problem, and it is
            // actually possible for the health to have changed in between as the
            // current best block might have been updated during the subscription
            // request.
            this.update(true);
            return null;
        }
        // Response doesn't concern us.
        if (parsedResponse.id) {
            const id = parsedResponse.id;
            // Need to remove the `extern:` prefix.
            if (!id.startsWith('extern:')) {
                throw new Error('State inconsistency in health checker');
            }
            const newId = JSON.parse(id.slice('extern:'.length));
            parsedResponse.id = newId;
        }
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(parsedResponse);
    };
    update = (startNow)=>{
        // If `startNow`, clear `#currentHealthTimeout` so that it is set below.
        if (startNow && this.#currentHealthTimeout) {
            clearTimeout(this.#currentHealthTimeout);
            this.#currentHealthTimeout = null;
        }
        if (!this.#currentHealthTimeout) {
            const startHealthRequest = ()=>{
                this.#currentHealthTimeout = null;
                // No matter what, don't start a health request if there is already one in progress.
                // This is sane to do because receiving a response to a health request calls `update()`.
                if (this.#currentHealthCheckId) {
                    return;
                }
                // Actual request starting.
                this.#currentHealthCheckId = `health-checker:${this.#nextRequestId}`;
                this.#nextRequestId += 1;
                this.#requestToSmoldot({
                    id: this.#currentHealthCheckId,
                    jsonrpc: '2.0',
                    method: 'system_health',
                    params: []
                });
            };
            if (startNow) {
                startHealthRequest();
            } else {
                this.#currentHealthTimeout = setTimeout(startHealthRequest, 1000);
            }
        }
        if (this.#isSyncing && !this.#currentSubscriptionId && !this.#currentSubunsubRequestId) {
            this.startSubscription();
        }
        if (!this.#isSyncing && this.#currentSubscriptionId && !this.#currentSubunsubRequestId) {
            this.endSubscription();
        }
    };
    startSubscription = ()=>{
        if (this.#currentSubunsubRequestId || this.#currentSubscriptionId) {
            throw new Error('Internal error in health checker');
        }
        this.#currentSubunsubRequestId = `health-checker:${this.#nextRequestId}`;
        this.#nextRequestId += 1;
        this.#requestToSmoldot({
            id: this.#currentSubunsubRequestId,
            jsonrpc: '2.0',
            method: 'chain_subscribeNewHeads',
            params: []
        });
    };
    endSubscription = ()=>{
        if (this.#currentSubunsubRequestId || !this.#currentSubscriptionId) {
            throw new Error('Internal error in health checker');
        }
        this.#currentSubunsubRequestId = `health-checker:${this.#nextRequestId}`;
        this.#nextRequestId += 1;
        this.#requestToSmoldot({
            id: this.#currentSubunsubRequestId,
            jsonrpc: '2.0',
            method: 'chain_unsubscribeNewHeads',
            params: [
                this.#currentSubscriptionId
            ]
        });
    };
    destroy = ()=>{
        if (this.#currentHealthTimeout) {
            clearTimeout(this.#currentHealthTimeout);
            this.#currentHealthTimeout = null;
        }
    };
}
class HealthCheckError extends Error {
    #cause;
    getCause() {
        return this.#cause;
    }
    constructor(response, message = 'Got error response asking for system health'){
        super(message);
        this.#cause = response;
    }
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/substrate-connect/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ScProvider",
    ()=>ScProvider
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.mjs [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.js [app-client] (ecmascript) <export default as EventEmitter>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/error.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/function.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$object$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/object.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/logger.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/noop.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/object/spread.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$substrate$2d$connect$2f$Health$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/substrate-connect/Health.js [app-client] (ecmascript)");
;
;
;
;
const l = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["logger"])('api-substrate-connect');
const subscriptionUnsubscriptionMethods = new Map([
    [
        'author_submitAndWatchExtrinsic',
        'author_unwatchExtrinsic'
    ],
    [
        'chain_subscribeAllHeads',
        'chain_unsubscribeAllHeads'
    ],
    [
        'chain_subscribeFinalizedHeads',
        'chain_unsubscribeFinalizedHeads'
    ],
    [
        'chain_subscribeFinalisedHeads',
        'chain_subscribeFinalisedHeads'
    ],
    [
        'chain_subscribeNewHeads',
        'chain_unsubscribeNewHeads'
    ],
    [
        'chain_subscribeNewHead',
        'chain_unsubscribeNewHead'
    ],
    [
        'chain_subscribeRuntimeVersion',
        'chain_unsubscribeRuntimeVersion'
    ],
    [
        'subscribe_newHead',
        'unsubscribe_newHead'
    ],
    [
        'state_subscribeRuntimeVersion',
        'state_unsubscribeRuntimeVersion'
    ],
    [
        'state_subscribeStorage',
        'state_unsubscribeStorage'
    ]
]);
const scClients = new WeakMap();
class ScProvider {
    #Sc;
    #coder = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RpcCoder"]();
    #spec;
    #sharedSandbox;
    #subscriptions = new Map();
    #resubscribeMethods = new Map();
    #requests = new Map();
    #wellKnownChains;
    #eventemitter = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__["EventEmitter"]();
    #chain = null;
    #isChainReady = false;
    constructor(Sc, spec, sharedSandbox){
        if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$object$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isObject"])(Sc) || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$object$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isObject"])(Sc.WellKnownChain) || !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$function$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isFunction"])(Sc.createScClient)) {
            throw new Error('Expected an @substrate/connect interface as first parameter to ScProvider');
        }
        this.#Sc = Sc;
        this.#spec = spec;
        this.#sharedSandbox = sharedSandbox;
        this.#wellKnownChains = new Set(Object.values(Sc.WellKnownChain));
    }
    get hasSubscriptions() {
        // Indicates that subscriptions are supported
        return !!true;
    }
    get isClonable() {
        return !!false;
    }
    get isConnected() {
        return !!this.#chain && this.#isChainReady;
    }
    clone() {
        throw new Error('clone() is not supported.');
    }
    // Config details can be found in @substrate/connect repo following the link:
    // https://github.com/paritytech/substrate-connect/blob/main/packages/connect/src/connector/index.ts
    async connect(config, checkerFactory = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$substrate$2d$connect$2f$Health$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["healthChecker"]) {
        if (this.isConnected) {
            throw new Error('Already connected!');
        }
        // it could happen that after emitting `disconnected` due to the fact that
        // smoldot is syncing, the consumer tries to reconnect after a certain amount
        // of time... In which case we want to make sure that we don't create a new
        // chain.
        if (this.#chain) {
            await this.#chain;
            return;
        }
        if (this.#sharedSandbox && !this.#sharedSandbox.isConnected) {
            await this.#sharedSandbox.connect();
        }
        const client = this.#sharedSandbox ? scClients.get(this.#sharedSandbox) : this.#Sc.createScClient(config);
        if (!client) {
            throw new Error('Unknown ScProvider!');
        }
        scClients.set(this, client);
        const hc = checkerFactory();
        const onResponse = (res)=>{
            const hcRes = hc.responsePassThrough(res);
            if (!hcRes) {
                return;
            }
            const response = JSON.parse(hcRes);
            let decodedResponse;
            try {
                decodedResponse = this.#coder.decodeResponse(response);
            } catch (e) {
                decodedResponse = e;
            }
            // It's not a subscription message, but rather a standar RPC response
            if (response.params?.subscription === undefined || !response.method) {
                return this.#requests.get(response.id)?.(decodedResponse);
            }
            // We are dealing with a subscription message
            const subscriptionId = `${response.method}::${response.params.subscription}`;
            const callback = this.#subscriptions.get(subscriptionId)?.[0];
            callback?.(decodedResponse);
        };
        const addChain = this.#sharedSandbox ? async (...args)=>{
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            const source = this.#sharedSandbox;
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            return (await source.#chain).addChain(...args);
        } : this.#wellKnownChains.has(this.#spec) ? client.addWellKnownChain : client.addChain;
        this.#chain = addChain(this.#spec, onResponse).then((chain)=>{
            hc.setSendJsonRpc(chain.sendJsonRpc);
            this.#isChainReady = false;
            const cleanup = ()=>{
                // If there are any callbacks left, we have to reject/error them.
                // Otherwise, that would cause a memory leak.
                const disconnectionError = new Error('Disconnected');
                this.#requests.forEach((cb)=>cb(disconnectionError));
                this.#subscriptions.forEach(([cb])=>cb(disconnectionError));
                this.#subscriptions.clear();
            };
            const staleSubscriptions = [];
            const killStaleSubscriptions = ()=>{
                if (staleSubscriptions.length === 0) {
                    return;
                }
                const stale = staleSubscriptions.pop();
                if (!stale) {
                    throw new Error('Unable to get stale subscription');
                }
                const { id, unsubscribeMethod } = stale;
                Promise.race([
                    this.send(unsubscribeMethod, [
                        id
                    ]).catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]),
                    new Promise((resolve)=>setTimeout(resolve, 500))
                ]).then(killStaleSubscriptions).catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
            };
            hc.start((health)=>{
                const isReady = !health.isSyncing && (health.peers > 0 || !health.shouldHavePeers);
                // if it's the same as before, then nothing has changed and we are done
                if (this.#isChainReady === isReady) {
                    return;
                }
                this.#isChainReady = isReady;
                if (!isReady) {
                    // If we've reached this point, that means that the chain used to be "ready"
                    // and now we are about to emit `disconnected`.
                    //
                    // This will cause the PolkadotJs API think that the connection is
                    // actually dead. In reality the smoldot chain is not dead, of course.
                    // However, we have to cleanup all the existing callbacks because when
                    // the smoldot chain stops syncing, then we will emit `connected` and
                    // the PolkadotJs API will try to re-create the previous
                    // subscriptions and requests. Although, now is not a good moment
                    // to be sending unsubscription messages to the smoldot chain, we
                    // should wait until is no longer syncing to send the unsubscription
                    // messages from the stale subscriptions of the previous connection.
                    //
                    // That's why -before we perform the cleanup of `this.#subscriptions`-
                    // we keep the necessary information that we will need later on to
                    // kill the stale subscriptions.
                    [
                        ...this.#subscriptions.values()
                    ].forEach((s)=>{
                        staleSubscriptions.push(s[1]);
                    });
                    cleanup();
                    this.#eventemitter.emit('disconnected');
                } else {
                    killStaleSubscriptions();
                    this.#eventemitter.emit('connected');
                    if (this.#resubscribeMethods.size) {
                        this.#resubscribe();
                    }
                }
            });
            return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["objectSpread"])({}, chain, {
                remove: ()=>{
                    hc.stop();
                    chain.remove();
                    cleanup();
                },
                sendJsonRpc: hc.sendJsonRpc.bind(hc)
            });
        });
        try {
            await this.#chain;
        } catch (e) {
            this.#chain = null;
            this.#eventemitter.emit('error', e);
            throw e;
        }
    }
    #resubscribe = ()=>{
        const promises = [];
        this.#resubscribeMethods.forEach((subDetails)=>{
            // only re-create subscriptions which are not in author (only area where
            // transactions are created, i.e. submissions such as 'author_submitAndWatchExtrinsic'
            // are not included (and will not be re-broadcast)
            if (subDetails.type.startsWith('author_')) {
                return;
            }
            try {
                const promise = new Promise((resolve)=>{
                    this.subscribe(subDetails.type, subDetails.method, subDetails.params, subDetails.callback).catch((error)=>console.log(error));
                    resolve();
                });
                promises.push(promise);
            } catch (error) {
                l.error(error);
            }
        });
        Promise.all(promises).catch((err)=>l.log(err));
    };
    async disconnect() {
        if (!this.#chain) {
            return;
        }
        const chain = await this.#chain;
        this.#chain = null;
        this.#isChainReady = false;
        try {
            chain.remove();
        } catch (_) {}
        this.#eventemitter.emit('disconnected');
    }
    on(type, sub) {
        // It's possible. Although, quite unlikely, that by the time that polkadot
        // subscribes to the `connected` event, the Provider is already connected.
        // In that case, we must emit to let the consumer know that we are connected.
        if (type === 'connected' && this.isConnected) {
            sub();
        }
        this.#eventemitter.on(type, sub);
        return ()=>{
            this.#eventemitter.removeListener(type, sub);
        };
    }
    async send(method, params) {
        if (!this.isConnected || !this.#chain) {
            throw new Error('Provider is not connected');
        }
        const chain = await this.#chain;
        const [id, json] = this.#coder.encodeJson(method, params);
        const result = new Promise((resolve, reject)=>{
            this.#requests.set(id, (response)=>{
                ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$error$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isError"])(response) ? reject : resolve)(response);
            });
            try {
                chain.sendJsonRpc(json);
            } catch (e) {
                this.#chain = null;
                try {
                    chain.remove();
                } catch (_) {}
                this.#eventemitter.emit('error', e);
            }
        });
        try {
            return await result;
        } finally{
            // let's ensure that once the Promise is resolved/rejected, then we remove
            // remove its entry from the internal #requests
            this.#requests.delete(id);
        }
    }
    async subscribe(type, method, params, callback) {
        if (!subscriptionUnsubscriptionMethods.has(method)) {
            throw new Error(`Unsupported subscribe method: ${method}`);
        }
        const id = await this.send(method, params);
        const subscriptionId = `${type}::${id}`;
        const cb = (response)=>{
            if (response instanceof Error) {
                callback(response, undefined);
            } else {
                callback(null, response);
            }
        };
        const unsubscribeMethod = subscriptionUnsubscriptionMethods.get(method);
        if (!unsubscribeMethod) {
            throw new Error('Invalid unsubscribe method found');
        }
        this.#resubscribeMethods.set(subscriptionId, {
            callback,
            method,
            params,
            type
        });
        this.#subscriptions.set(subscriptionId, [
            cb,
            {
                id,
                unsubscribeMethod
            }
        ]);
        return id;
    }
    unsubscribe(type, method, id) {
        if (!this.isConnected) {
            throw new Error('Provider is not connected');
        }
        const subscriptionId = `${type}::${id}`;
        if (!this.#subscriptions.has(subscriptionId)) {
            return Promise.reject(new Error(`Unable to find active subscription=${subscriptionId}`));
        }
        this.#resubscribeMethods.delete(subscriptionId);
        this.#subscriptions.delete(subscriptionId);
        return this.send(method, [
            id
        ]);
    }
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/ws/errors.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "getWSErrorString",
    ()=>getWSErrorString
]);
const known = {
    1000: 'Normal Closure',
    1001: 'Going Away',
    1002: 'Protocol Error',
    1003: 'Unsupported Data',
    1004: '(For future)',
    1005: 'No Status Received',
    1006: 'Abnormal Closure',
    1007: 'Invalid frame payload data',
    1008: 'Policy Violation',
    1009: 'Message too big',
    1010: 'Missing Extension',
    1011: 'Internal Error',
    1012: 'Service Restart',
    1013: 'Try Again Later',
    1014: 'Bad Gateway',
    1015: 'TLS Handshake'
};
function getWSErrorString(code) {
    if (code >= 0 && code <= 999) {
        return '(Unused)';
    } else if (code >= 1016) {
        if (code <= 1999) {
            return '(For WebSocket standard)';
        } else if (code <= 2999) {
            return '(For WebSocket extensions)';
        } else if (code <= 3999) {
            return '(For libraries and frameworks)';
        } else if (code <= 4999) {
            return '(For applications)';
        }
    }
    return known[code] || '(Unknown)';
}
}),
"[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/ws/index.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "WsProvider",
    ()=>WsProvider
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.mjs [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__ = __turbopack_context__.i("[project]/node_modules/eventemitter3/index.js [app-client] (ecmascript) <export default as EventEmitter>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$childClass$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/childClass.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$null$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/null.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/is/undefined.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/logger.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/noop.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/object/spread.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/util/stringify.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@polkadot/x-global/index.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/x-ws/browser.js [app-client] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/coder/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/defaults.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/lru.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$ws$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@snowbridge/api/node_modules/@polkadot/rpc-provider/ws/errors.js [app-client] (ecmascript)");
;
;
;
;
;
;
;
;
const ALIASES = {
    chain_finalisedHead: 'chain_finalizedHead',
    chain_subscribeFinalisedHeads: 'chain_subscribeFinalizedHeads',
    chain_unsubscribeFinalisedHeads: 'chain_unsubscribeFinalizedHeads'
};
const RETRY_DELAY = 2_500;
const DEFAULT_TIMEOUT_MS = 60 * 1000;
const TIMEOUT_INTERVAL = 5_000;
const l = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$logger$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["logger"])('api-ws');
/** @internal Clears a Record<*> of all keys, optionally with all callback on clear */ function eraseRecord(record, cb) {
    Object.keys(record).forEach((key)=>{
        if (cb) {
            cb(record[key]);
        }
        delete record[key];
    });
}
/** @internal Creates a default/empty stats object */ function defaultEndpointStats() {
    return {
        bytesRecv: 0,
        bytesSent: 0,
        cached: 0,
        errors: 0,
        requests: 0,
        subscriptions: 0,
        timeout: 0
    };
}
class WsProvider {
    #callCache;
    #coder;
    #endpoints;
    #headers;
    #eventemitter;
    #handlers = {};
    #isReadyPromise;
    #stats;
    #waitingForId = {};
    #cacheCapacity;
    #ttl;
    #autoConnectMs;
    #endpointIndex;
    #endpointStats;
    #isConnected = false;
    #subscriptions = {};
    #timeoutId = null;
    #websocket;
    #timeout;
    /**
     * @param {string | string[]} endpoint The endpoint url. Usually `ws://ip:9944` or `wss://ip:9944`, may provide an array of endpoint strings.
     * @param {number | false} autoConnectMs Whether to connect automatically or not (default). Provided value is used as a delay between retries.
     * @param {Record<string, string>} headers The headers provided to the underlying WebSocket
     * @param {number} [timeout] Custom timeout value used per request . Defaults to `DEFAULT_TIMEOUT_MS`
     * @param {number} [cacheCapacity] Custom size of the WsProvider LRUCache. Defaults to `DEFAULT_CAPACITY` (1024)
     * @param {number} [cacheTtl] Custom TTL of the WsProvider LRUCache. Determines how long an object can live in the cache. Defaults to DEFAULT_TTL` (30000)
     */ constructor(endpoint = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$defaults$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["default"].WS_URL, autoConnectMs = RETRY_DELAY, headers = {}, timeout, cacheCapacity, cacheTtl){
        const endpoints = Array.isArray(endpoint) ? endpoint : [
            endpoint
        ];
        if (endpoints.length === 0) {
            throw new Error('WsProvider requires at least one Endpoint');
        }
        endpoints.forEach((endpoint)=>{
            if (!/^(wss|ws):\/\//.test(endpoint)) {
                throw new Error(`Endpoint should start with 'ws://', received '${endpoint}'`);
            }
        });
        const ttl = cacheTtl === undefined ? __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_TTL"] : cacheTtl;
        this.#callCache = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["LRUCache"](cacheCapacity === 0 ? 0 : cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"], ttl);
        this.#ttl = cacheTtl;
        this.#cacheCapacity = cacheCapacity || __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$lru$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["DEFAULT_CAPACITY"];
        this.#eventemitter = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$eventemitter3$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__default__as__EventEmitter$3e$__["EventEmitter"]();
        this.#autoConnectMs = autoConnectMs || 0;
        this.#coder = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$coder$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["RpcCoder"]();
        this.#endpointIndex = -1;
        this.#endpoints = endpoints;
        this.#headers = headers;
        this.#websocket = null;
        this.#stats = {
            active: {
                requests: 0,
                subscriptions: 0
            },
            total: defaultEndpointStats()
        };
        this.#endpointStats = defaultEndpointStats();
        this.#timeout = timeout || DEFAULT_TIMEOUT_MS;
        if (autoConnectMs && autoConnectMs > 0) {
            this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
        }
        this.#isReadyPromise = new Promise((resolve)=>{
            this.#eventemitter.once('connected', ()=>{
                resolve(this);
            });
        });
    }
    /**
     * @summary `true` when this provider supports subscriptions
     */ get hasSubscriptions() {
        return !!true;
    }
    /**
     * @summary `true` when this provider supports clone()
     */ get isClonable() {
        return !!true;
    }
    /**
     * @summary Whether the node is connected or not.
     * @return {boolean} true if connected
     */ get isConnected() {
        return this.#isConnected;
    }
    /**
     * @description Promise that resolves the first time we are connected and loaded
     */ get isReady() {
        return this.#isReadyPromise;
    }
    get endpoint() {
        return this.#endpoints[this.#endpointIndex];
    }
    /**
     * @description Returns a clone of the object
     */ clone() {
        return new WsProvider(this.#endpoints);
    }
    selectEndpointIndex(endpoints) {
        return (this.#endpointIndex + 1) % endpoints.length;
    }
    /**
     * @summary Manually connect
     * @description The [[WsProvider]] connects automatically by default, however if you decided otherwise, you may
     * connect manually using this method.
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async connect() {
        if (this.#websocket) {
            throw new Error('WebSocket is already connected');
        }
        try {
            this.#endpointIndex = this.selectEndpointIndex(this.#endpoints);
            // the as here is Deno-specific - not available on the globalThis
            this.#websocket = typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["xglobal"].WebSocket !== 'undefined' && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$childClass$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isChildClass"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$x$2d$global$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["xglobal"].WebSocket, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"]) ? new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"](this.endpoint) : new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$x$2d$ws$2f$browser$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$locals$3e$__["WebSocket"](this.endpoint, undefined, {
                headers: this.#headers
            });
            if (this.#websocket) {
                this.#websocket.onclose = this.#onSocketClose;
                this.#websocket.onerror = this.#onSocketError;
                this.#websocket.onmessage = this.#onSocketMessage;
                this.#websocket.onopen = this.#onSocketOpen;
            }
            // timeout any handlers that have not had a response
            this.#timeoutId = setInterval(()=>this.#timeoutHandlers(), TIMEOUT_INTERVAL);
        } catch (error) {
            l.error(error);
            this.#emit('error', error);
            throw error;
        }
    }
    /**
     * @description Connect, never throwing an error, but rather forcing a retry
     */ async connectWithRetry() {
        if (this.#autoConnectMs > 0) {
            try {
                await this.connect();
            } catch  {
                setTimeout(()=>{
                    this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
                }, this.#autoConnectMs);
            }
        }
    }
    /**
     * @description Manually disconnect from the connection, clearing auto-connect logic
     */ // eslint-disable-next-line @typescript-eslint/require-await
    async disconnect() {
        // switch off autoConnect, we are in manual mode now
        this.#autoConnectMs = 0;
        try {
            if (this.#websocket) {
                // 1000 - Normal closure; the connection successfully completed
                this.#websocket.close(1000);
            }
        } catch (error) {
            l.error(error);
            this.#emit('error', error);
            throw error;
        }
    }
    /**
     * @description Returns the connection stats
     */ get stats() {
        return {
            active: {
                requests: Object.keys(this.#handlers).length,
                subscriptions: Object.keys(this.#subscriptions).length
            },
            total: this.#stats.total
        };
    }
    /**
    * @description Returns the connection stats
    */ get ttl() {
        return this.#ttl;
    }
    get endpointStats() {
        return this.#endpointStats;
    }
    /**
     * @summary Listens on events after having subscribed using the [[subscribe]] function.
     * @param  {ProviderInterfaceEmitted} type Event
     * @param  {ProviderInterfaceEmitCb}  sub  Callback
     * @return unsubscribe function
     */ on(type, sub) {
        this.#eventemitter.on(type, sub);
        return ()=>{
            this.#eventemitter.removeListener(type, sub);
        };
    }
    /**
     * @summary Send JSON data using WebSockets to configured HTTP Endpoint or queue.
     * @param method The RPC methods to execute
     * @param params Encoded parameters as applicable for the method
     * @param subscription Subscription details (internally used)
     */ send(method, params, isCacheable, subscription) {
        this.#endpointStats.requests++;
        this.#stats.total.requests++;
        const [id, body] = this.#coder.encodeJson(method, params);
        if (this.#cacheCapacity === 0) {
            return this.#send(id, body, method, params, subscription);
        }
        const cacheKey = isCacheable ? `${method}::${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$stringify$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["stringify"])(params)}` : '';
        let resultPromise = isCacheable ? this.#callCache.get(cacheKey) : null;
        if (!resultPromise) {
            resultPromise = this.#send(id, body, method, params, subscription);
            if (isCacheable) {
                this.#callCache.set(cacheKey, resultPromise);
            }
        } else {
            this.#endpointStats.cached++;
            this.#stats.total.cached++;
        }
        return resultPromise;
    }
    async #send(id, body, method, params, subscription) {
        return new Promise((resolve, reject)=>{
            try {
                if (!this.isConnected || this.#websocket === null) {
                    throw new Error('WebSocket is not connected');
                }
                const callback = (error, result)=>{
                    error ? reject(error) : resolve(result);
                };
                l.debug(()=>[
                        'calling',
                        method,
                        body
                    ]);
                this.#handlers[id] = {
                    callback,
                    method,
                    params,
                    start: Date.now(),
                    subscription
                };
                const bytesSent = body.length;
                this.#endpointStats.bytesSent += bytesSent;
                this.#stats.total.bytesSent += bytesSent;
                this.#websocket.send(body);
            } catch (error) {
                this.#endpointStats.errors++;
                this.#stats.total.errors++;
                const rpcError = error;
                const failedRequest = `\nFailed WS Request: ${JSON.stringify({
                    method,
                    params
                })}`;
                // Provide WS Request alongside the error
                rpcError.message = `${rpcError.message}${failedRequest}`;
                reject(rpcError);
            }
        });
    }
    /**
     * @name subscribe
     * @summary Allows subscribing to a specific event.
     *
     * @example
     * <BR>
     *
     * ```javascript
     * const provider = new WsProvider('ws://127.0.0.1:9944');
     * const rpc = new Rpc(provider);
     *
     * rpc.state.subscribeStorage([[storage.system.account, <Address>]], (_, values) => {
     *   console.log(values)
     * }).then((subscriptionId) => {
     *   console.log('balance changes subscription id: ', subscriptionId)
     * })
     * ```
     */ subscribe(type, method, params, callback) {
        this.#endpointStats.subscriptions++;
        this.#stats.total.subscriptions++;
        // subscriptions are not cached, LRU applies to .at(<blockHash>) only
        return this.send(method, params, false, {
            callback,
            type
        });
    }
    /**
     * @summary Allows unsubscribing to subscriptions made with [[subscribe]].
     */ async unsubscribe(type, method, id) {
        const subscription = `${type}::${id}`;
        // FIXME This now could happen with re-subscriptions. The issue is that with a re-sub
        // the assigned id now does not match what the API user originally received. It has
        // a slight complication in solving - since we cannot rely on the send id, but rather
        // need to find the actual subscription id to map it
        if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(this.#subscriptions[subscription])) {
            l.debug(()=>`Unable to find active subscription=${subscription}`);
            return false;
        }
        delete this.#subscriptions[subscription];
        try {
            return this.isConnected && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$null$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isNull"])(this.#websocket) ? this.send(method, [
                id
            ]) : true;
        } catch  {
            return false;
        }
    }
    #emit = (type, ...args)=>{
        this.#eventemitter.emit(type, ...args);
    };
    #onSocketClose = (event)=>{
        const error = new Error(`disconnected from ${this.endpoint}: ${event.code}:: ${event.reason || (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$snowbridge$2f$api$2f$node_modules$2f40$polkadot$2f$rpc$2d$provider$2f$ws$2f$errors$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["getWSErrorString"])(event.code)}`);
        if (this.#autoConnectMs > 0) {
            l.error(error.message);
        }
        this.#isConnected = false;
        if (this.#websocket) {
            this.#websocket.onclose = null;
            this.#websocket.onerror = null;
            this.#websocket.onmessage = null;
            this.#websocket.onopen = null;
            this.#websocket = null;
        }
        if (this.#timeoutId) {
            clearInterval(this.#timeoutId);
            this.#timeoutId = null;
        }
        // reject all hanging requests
        eraseRecord(this.#handlers, (h)=>{
            try {
                h.callback(error, undefined);
            } catch (err) {
                // does not throw
                l.error(err);
            }
        });
        eraseRecord(this.#waitingForId);
        // Reset stats for active endpoint
        this.#endpointStats = defaultEndpointStats();
        this.#emit('disconnected');
        if (this.#autoConnectMs > 0) {
            setTimeout(()=>{
                this.connectWithRetry().catch(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$noop$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["noop"]);
            }, this.#autoConnectMs);
        }
    };
    #onSocketError = (error)=>{
        l.debug(()=>[
                'socket error',
                error
            ]);
        this.#emit('error', error);
    };
    #onSocketMessage = (message)=>{
        l.debug(()=>[
                'received',
                message.data
            ]);
        const bytesRecv = message.data.length;
        this.#endpointStats.bytesRecv += bytesRecv;
        this.#stats.total.bytesRecv += bytesRecv;
        const response = JSON.parse(message.data);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$is$2f$undefined$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["isUndefined"])(response.method) ? this.#onSocketMessageResult(response) : this.#onSocketMessageSubscribe(response);
    };
    #onSocketMessageResult = (response)=>{
        const handler = this.#handlers[response.id];
        if (!handler) {
            l.debug(()=>`Unable to find handler for id=${response.id}`);
            return;
        }
        try {
            const { method, params, subscription } = handler;
            const result = this.#coder.decodeResponse(response);
            // first send the result - in case of subs, we may have an update
            // immediately if we have some queued results already
            handler.callback(null, result);
            if (subscription) {
                const subId = `${subscription.type}::${result}`;
                this.#subscriptions[subId] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$polkadot$2f$util$2f$object$2f$spread$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["objectSpread"])({}, subscription, {
                    method,
                    params
                });
                // if we have a result waiting for this subscription already
                if (this.#waitingForId[subId]) {
                    this.#onSocketMessageSubscribe(this.#waitingForId[subId]);
                }
            }
        } catch (error) {
            this.#endpointStats.errors++;
            this.#stats.total.errors++;
            handler.callback(error, undefined);
        }
        delete this.#handlers[response.id];
    };
    #onSocketMessageSubscribe = (response)=>{
        if (!response.method) {
            throw new Error('No method found in JSONRPC response');
        }
        const method = ALIASES[response.method] || response.method;
        const subId = `${method}::${response.params.subscription}`;
        const handler = this.#subscriptions[subId];
        if (!handler) {
            // store the JSON, we could have out-of-order subid coming in
            this.#waitingForId[subId] = response;
            l.debug(()=>`Unable to find handler for subscription=${subId}`);
            return;
        }
        // housekeeping
        delete this.#waitingForId[subId];
        try {
            const result = this.#coder.decodeResponse(response);
            handler.callback(null, result);
        } catch (error) {
            this.#endpointStats.errors++;
            this.#stats.total.errors++;
            handler.callback(error, undefined);
        }
    };
    #onSocketOpen = ()=>{
        if (this.#websocket === null) {
            throw new Error('WebSocket cannot be null in onOpen');
        }
        l.debug(()=>[
                'connected to',
                this.endpoint
            ]);
        this.#isConnected = true;
        this.#resubscribe();
        this.#emit('connected');
        return true;
    };
    #resubscribe = ()=>{
        const subscriptions = this.#subscriptions;
        this.#subscriptions = {};
        Promise.all(Object.keys(subscriptions).map(async (id)=>{
            const { callback, method, params, type } = subscriptions[id];
            // only re-create subscriptions which are not in author (only area where
            // transactions are created, i.e. submissions such as 'author_submitAndWatchExtrinsic'
            // are not included (and will not be re-broadcast)
            if (type.startsWith('author_')) {
                return;
            }
            try {
                await this.subscribe(type, method, params, callback);
            } catch (error) {
                l.error(error);
            }
        })).catch(l.error);
    };
    #timeoutHandlers = ()=>{
        const now = Date.now();
        const ids = Object.keys(this.#handlers);
        for(let i = 0, count = ids.length; i < count; i++){
            const handler = this.#handlers[ids[i]];
            if (now - handler.start > this.#timeout) {
                try {
                    handler.callback(new Error(`No response received from RPC endpoint in ${this.#timeout / 1000}s`), undefined);
                } catch  {
                // ignore
                }
                this.#endpointStats.timeout++;
                this.#stats.total.timeout++;
                delete this.#handlers[ids[i]];
            }
        }
    };
}
}),
]);

//# sourceMappingURL=node_modules_8eb02711._.js.map